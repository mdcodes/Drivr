<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item
            name="android:windowBackground">@drawable/drivr_launcher</item>
    </style>

    <style name="AppTheme" parent="AppBaseTheme">
    </style>

    <style name="LikeButton">
        <item name="android:background">@drawable/custombutton</item>
        <item name="android:color">@color/btnLike</item>
    </style>

    <style name="DislikeButton">
        <item name="android:background">@drawable/custombutton</item>
        <item name="android:color">@color/btnDislike</item>
    </style>

    <style name="FullscreenTheme" parent="AppBaseTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

</resources>
